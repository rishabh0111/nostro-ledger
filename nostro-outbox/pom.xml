<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.nostro</groupId>
    <artifactId>nostro-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>nostro-outbox</artifactId>
  <name>Nostro :: Outbox</name>
  <description>What the relay publishes: the EntryRecorded message, and the topic it travels on.</description>

  <!--
    The contract between three deployables: the API service writes it into the outbox, the relay
    publishes it, the projection applies it. May depend on the domain and a JSON mapper. Must not
    depend on either database's schema, on Kafka, or on anything that runs.
  -->
  <dependencies>
    <dependency>
      <groupId>io.nostro</groupId>
      <artifactId>nostro-domain</artifactId>
    </dependency>
    <dependency>
      <groupId>tools.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
    </dependency>

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
